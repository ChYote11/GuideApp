import 'package:flutter/material.dart';

class MyTopNavBar extends StatelessWidget {
  const MyTopNavBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}